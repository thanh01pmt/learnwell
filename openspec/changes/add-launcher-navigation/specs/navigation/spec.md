## ADDED Requirements

### Requirement: Platform Launcher Entry
The application SHALL provide a visible entry point to the Platform Launcher (`/code/launcher`) in the sidebar navigation for relevant user roles (Student, Teacher).

#### Scenario: Navigate to Launcher from Sidebar
- **WHEN** a user clicks the "Platform Launcher" item in the sidebar
- **THEN** the application redirects the user to the `/code/launcher` route
